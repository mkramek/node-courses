const signin = (credentials) => {
  // TODO: Logowanie (interakcja z bazą danych/JWT)
}

const signout = () => {
  // TODO: Wylogowanie (interakcja z JWT)
}

const signup = (user) => {
  // TODO: Rejestracja (interakcja z bazą danych)
}

module.exports = {
    signin,
    signout,
    signup
}